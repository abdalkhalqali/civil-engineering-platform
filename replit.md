# Civil Engineering Platform

## تشغيل المعاينة

شغّل Workflow `Flutter Web Preview` من Replit، أو نفّذ من جذر المشروع:

```bash
./scripts/start_flutter_web.sh
```

يُبنى تطبيق Flutter Web على المنفذ `5000`. السكربت يجهّز Flutter 3.47.4،
يبني نواة Rust إلى WebAssembly، ثم يضيف ترويسات العزل المطلوبة لـ
`flutter_rust_bridge`.

## مساحة العمل ثلاثية الأبعاد

تبدأ الواجهة من `apps/client_flutter/lib/screens/workbench_screen.dart`.
المشهد الهندسي التفاعلي موجود في
`apps/client_flutter/lib/widgets/engineering_viewport.dart`، وهو رسم ثلاثي
الأبعاد فعلي عبر إسقاط نقاط العالم إلى Canvas، وليس صورة ثابتة أو Mockup.

يدعم الـViewport حاليًا:

- شبكة هندسية ومحاور X/Y/Z مع تسميات الشبكة (A, B, 1, 2).
- Orbit بالسحب، وPan من خلال وضع التحريك، وZoom بعجلة الماوس.
- Perspective وOrthographic.
- مجسمات ثلاثية الأبعاد حقيقية لأعمدة وكمرات وبلاطات وجدران وأساسات.
- تحديد العناصر بالنقر مع عرض خصائصها في لوحة المستكشف.
- أداة إنشاء الأعمدة والنقر على نقطة في المشهد.
- أداة رسم الكمرة بين نقطتين.
- أداة رسم الجدار بين نقطتين.
- أداة إنشاء البلاطة مع حواري الأبعاد.
- أداة إنشاء الأساس مع حواري الأبعاد.
- حذف العناصر المحددة.
- التراجع والإعادة.
- الإحداثيات الحية في شريط الحالة.
- الالتقاط إلى تقاطعات الشبكة.
- الإدخال الرقمي الدقيق للإحداثيات.
- مرشحات إظهار وإخفاء فئات العناصر، ووحدة عرض قابلة للتغيير.
- على الهاتف يظهر زر «لوحة الخصائص» لفتح مستكشف المشروع من الجانب.
- تسميات المستويات في المشهد ثلاثي الأبعاد.
- شريط أدوات عائم متكيف (هاتف/لوحي/حاسوب) مع جميع أدوات النمذجة والتعديل والعرض.
- بحث سريع عن الأدوات عبر Ctrl+K مع إدخال اسم الأداة بالعربية أو الإنجليزية.
- لوحة إدخال ديناميكية تظهر أثناء رسم الكمرة/الجدار مع القياسات اللحظية.
- مؤشرات الالتقاط (Snap) مع نقاط النهاية والمنتصف والتقاطعات والمحاذاة.
- قيود ذكية (أفقي، رأسي، متوازي، متعامد) أثناء الرسم.
- إنشاء مستويات متعددة بسرعة (عدد + ارتفاع).
- نسخ العناصر إلى مستويات أخرى.
- اختصارات لوحة المفاتيح: Ctrl+K بحث، Ctrl+Z تراجع، Ctrl+S حفظ، Escape إلغاء.
- مفاتيح أرقام 1-6 للتبديل السريع بين الأدوات.
- أزرار تعديل وحذف العناصر المحددة.
- خيارات العرض: منظوري/متعامد مع تبديل سريع.

تظهر حالة اتصال Rust في رأس الشاشة بعد نجاح `RustLib.init()`.

## التحقق

```bash
cd apps/client_flutter
flutter analyze
flutter test
```

لاختبارات الجسر على جهاز التطوير، يجب أولًا بناء المكتبة محليًا ثم تمرير مسارها:

```bash
cd core/geometry_kernel_rs && cargo build
cd ../../apps/client_flutter
FRB_DART_LOAD_EXTERNAL_LIBRARY_NATIVE_LIB_DIR=../../core/geometry_kernel_rs/target/debug flutter test
```